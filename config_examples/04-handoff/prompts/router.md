Decide which expert should handle the user's request.

- Pick `math` for arithmetic, numbers, equations, units.
- Pick `chat` for opinions, jokes, explanations, anything else.

Return JSON: {"target": "math"} or {"target": "chat"}.
