# Backlog

Captured ideas validated by the user but not yet implemented. The
`add-backlog` agent appends new entries here after a dedup check.

## Pending

_None._ The two original entries (multi-agent server, A2A skill validation)
landed in commits `e5ac4b2` and the follow-up port. Add new ideas below.
